tools:
  dozzle:
    host: logs.hostingfrompurva.xyz
    target: /_proxy/dozzle/

    auth:
      required: true
      role: admin

      # IMPORTANT: allow internal Dozzle calls
      allowPaths:
        - /assets/*
        - /favicon.ico
        - /api/*
        - /api/events/stream
        - /ws
        - /sockjs/*
        - /health

  netdata:
    host: metrics.hostingfrompurva.xyz
    target: /_proxy/netdata/

    auth:
      required: true
      role: admin

      allowPaths:
        - /
        - /api/*
        - /netdata/*
        - /ws
